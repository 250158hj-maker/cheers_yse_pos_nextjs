# CLAUDE.md

## プロジェクト概要

飲食店向け小規模POSシステム（YSEレジシステム）のNext.js実装。
PHPチーム開発版と同一の要件・設計をベースに、個人練習および卒業制作本番スタックの習熟を目的とした個人開発プロジェクト。

---

## 設計書

実装はすべて以下の設計書に基づいて行う。実装前に必ず該当箇所を確認すること。

```
docs/
├── 要件定義書.md   # システムの目的・機能要件・スコープ外・非機能要件
└── 基本設計書.md   # 画面一覧・画面遷移・ルーティング・ディレクトリ構成・ER図（Next.js版）
```

---

## 技術スタック

| 項目 | 内容 |
|---|---|
| フロントエンド | Next.js + TypeScript |
| バックエンド | Next.js API Routes（モノレポ構成） |
| データベース | PostgreSQL + Prisma |
| 認証 | Auth.js |
| 開発環境 | Docker + docker-compose |
| デプロイ | Vercel + Supabase |
| 動作ブラウザ | Google Chrome（最新版）のみ |

---

## ディレクトリ構成の原則

```
cheers_yse_pos/
├── app/
│   ├── page.tsx              # SCR-01 ログイン画面
│   ├── register/page.tsx     # SCR-02 レジ画面
│   ├── admin/page.tsx        # SCR-03 売上管理画面
│   ├── admin/products/       # SCR-04 商品設定画面
│   └── api/                  # APIエンドポイント（route.ts）
├── lib/                      # ビジネスロジック（db.ts・auth.ts・validations/）
├── prisma/schema.prisma      # テーブル定義・マイグレーション管理
├── proxy.ts             # 認証ガード・ロール別リダイレクト
└── .env.local                # 環境変数（Git管理外・絶対にコミットしない）
```

- ビジネスロジックは `lib/` に置く。`app/` のファイルに直接書かない
- 認証ガードは `proxy.ts` に集約する。各 `page.tsx` に認証チェックを書かない
- DB操作は `lib/db.ts` のPrismaクライアントを通して行う。直接クエリを `app/` に書かない

---

## 言語・コミットルール

- **会話・コードコメント・コミットメッセージはすべて日本語**で書く
- コミットメッセージの形式：`種別: 内容`

```
# 種別の例
feat: 新しい機能の追加
fix: 不具合の修正
docs: ドキュメントのみの変更
style: フォーマットの修正
refactor: リファクタリング・構造の変更
chore: 環境設定・依存関係など

# 例
feat: 商品登録フォームを追加
fix: 消費税計算の丸め誤差を修正
```

---

## 実装前の確認ルール

**実装に入る前に必ず以下を説明し、許可が出るまでコードを書き始めない。**
1機能ごとに完全に止まり、許可を待つ。複数機能を一度に実装しない。

```
【実装前確認】
1. 何を実装するか（対象ファイル・機能の概要）
2. どういう方針で実装するか（設計上の判断ポイント）
3. 設計書を参照しても不明瞭な点があれば明示する
```

設計書と実装の間に矛盾が生じた場合は**実装を止めて報告する**。自己判断で設計を変えない。

---

## コード品質の原則

### データ管理

- 同じデータを複数箇所で管理しない（Single Source of Truth）
- URLパラメータで状態を管理できる場合は `useState` より優先する（リロード後も状態が保たれ、URLの共有＝状態の共有になる）
- 導出できる値は必ず導出する。二重管理はバグの温床になる

### 責任の分離

- Server ComponentとClient Componentの責任を明確にする
  - データ取得・DBアクセスはServer Componentで行う
  - インタラクション（イベント・状態管理）が必要な部分だけ `'use client'` にする
- `'use server'` と `'use client'` はセキュリティ上の意味を持つ。必要な箇所にだけ付与する
- 1つの関数は1つのことだけ行う
- 変数・関数名は意図が伝わる名前にする（`data`、`temp`、`flag` などは使わない）

### 型安全

- `any` を使わない。型が不明な場合は `unknown` を使い、適切に型ガードする
- Prismaが生成する型を積極的に使う。独自の型定義と二重管理しない
- 入力値のバリデーションは `lib/validations/` のzodスキーマを使う

### エラーハンドリング

- `catch` の中を空にしない
- APIルートはすべて適切なHTTPステータスコードを返す（成功: 200/201、不正: 400、未認証: 401、未発見: 404、サーバーエラー: 500）
- エラーをコンソール出力だけで握りつぶさない

### コメント

コードを読めばわかることは書かない。**なぜそう実装したか（理由・背景）**を書く。

```typescript
// NG: コードそのままの説明
// price を 0 にセット
const price = 0;

// OK: 判断の根拠を残す
// 金額は円単位の整数で管理。小数点以下はアプリ層で丸めてから保存し、計算誤差の蓄積を防ぐ
const price = 0;
```

---

## 認証・セキュリティルール

- 認証セッションの取得はサーバーサイドで `getServerSession()` を使う
- ミドルウェアでのロール判定：
  - スタッフロール → `/register` のみアクセス可
  - 管理者ロール → `/admin` 以下のみアクセス可
  - 未認証 → `/`（ログイン画面）にリダイレクト
- クライアントサイドで認証チェックを行わない（ミドルウェアに集約する）

---

## Prismaルール

- スキーマ変更後は必ず `prisma migrate dev` を実行する
- マイグレーションファイルはコミットする（チームで共有するため）
- `prisma.ts` のクライアントインスタンスは `lib/db.ts` の1箇所だけで生成する

---

## 環境変数

以下を `.env.local` に定義する。コードに直接書かない。

```
DATABASE_URL=
NEXTAUTH_SECRET=
NEXTAUTH_URL=
```

---

## スコープ外（実装しない機能）

設計書で明示的にスコープ外とされている以下の機能は実装しない。

- 複数管理者アカウントの管理
- 在庫管理機能
- レシート印刷
- レジドロワーとの連携
- 売上の高度な分析・グラフ表示
- スマートフォン・タブレット最適化
- キャッシュレス決済の連携

---

## 禁止事項

| 禁止 | 理由 |
|---|---|
| 設計書にない機能の追加 | スコープ管理の破壊につながる |
| 環境変数のハードコード | 認証情報・接続情報の漏洩リスク |
| `any` 型の使用 | TypeScriptの型安全性を無効化する |
| `console.log` のコミット残し | 本番環境への混入リスク |
| 複数機能の同時実装 | デバッグ・確認が困難になる |
| 自己判断での設計変更 | 決定の記録が失われる |
| クライアントサイドでの認証チェック | ミドルウェアへの集約が崩れる |
